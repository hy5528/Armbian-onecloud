
name: "Build Armbian"
on:
  workflow_dispatch:
jobs:
  build-armbian:
    runs-on: ubuntu-latest
    steps:
      - uses: soulteary/armbian-build-s805@codebase-merge-witallwang
        with:
          armbian_token:     "${{ secrets.GITHUB_TOKEN }}"  # GitHub token
          armbian_release:   "bullseye"                        # userspace
          armbian_target:    "build"                        # build=image, kernel=kernel
          armbian_board:     "aml-s812"                   # build target
